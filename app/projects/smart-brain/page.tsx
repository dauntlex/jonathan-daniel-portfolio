import Link from 'next/link'
import {projects} from '../../../data/site'
export default function SmartBrain(){const p=projects.find(x=>x.slug==='smart-brain')!;return <main><section className="case-hero wrap"><Link href="/projects" className="back">← All projects</Link><p className="eyebrow">{p.number} · {p.category}</p><h1>{p.title}</h1><p>{p.description}</p><div className="tags">{p.stack.map(x=><span key={x}>{x}</span>)}</div></section><section className="wrap case-media"><img src={p.image} alt="Smart Brain screenshot placeholder"/></section><section className="wrap case-content"><div><p className="eyebrow">The project</p><h2>Full-stack face recognition application.</h2></div><div className="prose"><p>The supplied resume describes Smart Brain as a ZTM Web Development project built with React, Node.js, Express.js, PostgreSQL and REST APIs.</p><p>The application includes a responsive UI, image recognition integration, user-data APIs and Git-based version control.</p><p className="note">Case-study sections for challenge, process, screenshots, results and live links are ready to fill in when those project details are available.</p></div></section></main>}