'use client'
import {useEffect,useState} from 'react'
import Link from 'next/link'
type Draft={title:string;category:string;description:string;image:string}
const empty:Draft={title:'',category:'Product Design',description:'',image:''}
export default function Admin(){const [draft,setDraft]=useState<Draft>(empty);const [saved,setSaved]=useState<Draft[]>([]);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem('portfolio-drafts')||'[]'))}catch{}},[]);function save(){const next=[...saved,draft];setSaved(next);localStorage.setItem('portfolio-drafts',JSON.stringify(next));setDraft(empty)}function file(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setDraft(x=>({...x,image:String(r.result)}));r.readAsDataURL(f)}return <main className="admin wrap"><div className="admin-top"><div><p className="eyebrow">Portfolio Admin</p><h1>Manage projects.</h1><p className="muted">A simple drag-and-drop style project manager. Drafts are stored locally for now; permanent publishing storage can be connected next.</p></div><Link className="button outline" href="/">← Back to site</Link></div><div className="admin-grid"><section className="admin-card"><h2>Add a project</h2><label>Project title<input value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})}/></label><label>Category<input value={draft.category} onChange={e=>setDraft({...draft,category:e.target.value})}/></label><label>Description<textarea rows={5} value={draft.description} onChange={e=>setDraft({...draft,description:e.target.value})}/></label><label>Cover image<input type="file" accept="image/*" onChange={file}/></label>{draft.image&&<img className="admin-preview" src={draft.image} alt="Preview"/>}<button className="button dark" onClick={save} disabled={!draft.title||!draft.image}>Save draft</button></section><section className="admin-card"><h2>Draft projects</h2>{saved.length===0?<p className="muted">No drafts yet. Add your next project here.</p>:saved.map((p,i)=><article className="draft" key={i}>{p.image&&<img src={p.image} alt=""/>}<div><strong>{p.title}</strong><span>{p.category}</span><p>{p.description}</p></div></article>)}</section></div></main>}
